import type { FC } from 'react';
import { Box, Container } from '@mui/material';

const FooterFC: FC = () => {
    return (
        <>
            <Box>footer</Box>
        </>
    );
};

export default FooterFC;
